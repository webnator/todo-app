{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": ["coverage/**"],
      "inputs": ["src/**/*.ts", "test/**/*.ts"],
      "outputMode": "full"
    },
    "lint": {}
  }
}
